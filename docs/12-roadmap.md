# 12. Дорожная карта развития

**Статус:** утверждено v1.50 (добавлена задача этапа 2 «Актуальность рыночных данных в реальном времени» — ТЗ docs/24, статус «на согласовании»)
**Система:** NewsTrader Assistant

Этапы развития системы. Сроки ориентировочные и зависят от размера команды. Реализовано (этап 2): **индикаторы EMA и MACD** (§8.1–8.2 docs/19) — модули, реестр/API `/v1/indicators/ema|macd`, веб-вкладки на `/indicators`, интеграция MACD в движок стратегий (техсигнал тренда). **P1-индикаторы Боллинджер (BB), ATR, ADX/DI реализованы** (§8.3/8.8/8.10 docs/19): модули, реестр/API `/v1/indicators/bollinger|atr|adx`, веб-вкладки на `/indicators`, интеграция в движок стратегий (BB — зоны перекупленности/перепроданности, kind `bollinger`; ATR — контекст волатильности, kind `atr`; ADX — фильтр силы тренда, kind `adx`).

## Этап 0. Фундамент (MVP)

**Цель:** проверить ключевую гипотезу — система умеет давать обоснованный вердикт по бумаге.

**Фокус среза:** рынок Мосбиржи; сектора авиация + нефть (AFLT, LKOH, GAZP и связанные бумаги). Стек: Python + FastAPI + PostgreSQL, граф в таблицах, NLP через LLM API (DeepSeek → OpenAI).

| Задача | Результат | Статус |
|---|---|---|
| Конвейер сбора новостей: RSS российских агентств + Telegram-каналы | Работающий сбор, нормализация, дедупликация; автоматический запуск по расписанию | RSS ✅ / Telegram (источник) ✅ |
| NLP-анализ через LLM: тематика, тональность, сущности, факты | Структурированный анализ новостей (русский язык) | ✅ |
| Базовый knowledge graph: 50–100 сущностей и курируемых связей | Цепочки влияния по отраслям | ✅ (66 сущностей, 85 связей) |
| Биржевые данные через MOEX ISS + базовые индикаторы | Актуальные котировки и индикаторы по целевому списку | ✅ |
| Strategy Engine первой версии + обоснование | Вердикты BUY/SELL/HOLD/INSUFFICIENT_DATA | ✅ (калибровка проход 1) |
| REST API + минимальный веб-интерфейс + Telegram-бот | Запрос аналитики по тикеру тремя способами | ✅ / ✅ / ✅ |
| Инфраструктура: Docker (Postgres + Liquibase), планировщик | Воспроизводимое развёртывание и контроль схемы | ✅ |

**Критерий выхода:** пользователь получает обоснованный вердикт с цитатами и ссылками по интересующей бумаге; метрики качества NFR-04 достигнуты на валидационном наборе. Оценка качества накапливается через бэктест-харнесс ([13-operations.md](./13-operations.md)).

## Этап 1. Продуктовое ядро

**Цель:** полноценное пользовательское использование.

| Задача | Результат |
|---|---|
| Watchlist, портфель, история запросов | Полный цикл пользователя (UC-01…UC-05) |
| Алерты по значимым новостям | Уведомления по настройкам |
| Макрокалендарь и привязка к бумагам | Контекст событий в вердиктах |
| Расширение источников: Telegram, сайты компаний, английский язык | Шире охват, мультиязычность |
| **Бэкфилл исторических новостей:** RSS (глубина ограничена выдачей ленты — обычно дни/недели), архив и поиск Telegram-каналов (Telethon, глубокая история), архивы/поисковые API источников | Быстрый старт ассистента и исторические бэктесты на прошлых периодах |
| Обратная связь «сработало/не сработало» | Статистика качества в интерфейсе |
| Бэктестинг вердиктов «на момент T» | Отчёты о качестве за периоды |

**Статус:** начат. Реализованы срезы 1–6: авторизация (регистрация/вход/сессии), watchlist, портфель с переоценкой по MOEX и P&L, история стратегий, алерты по значимым новостям (генерация в конвейере, API и веб-страница с настройками), push-доставка алертов в Telegram (привязка чата по коду `/link`, доставка в конвейере), макрокалендарь (модель данных, API, веб-страница и блок на карточке бумаги, наполнение), обратная связь «сработало/частично/нейтрально/не сработало» (API, страница истории со статистикой качества, кнопки на карточке и при закрытии позиции), Telegram-источник новостей (Telethon, `--telegram` в сборе и конвейере), **бэктестинг вердиктов «на момент T»** (`scripts/backtest_asof.py`: реконструкция вердикта только по данным, доступным в T, отчёт о качестве за периоды; устранено заглядывание в будущее в движке стратегий), **News Sources Manager** (страница `/news`, персональные списки RSS-лент с категориями и статусами проверки ✓/✗, ручное добавление и LLM-поиск лент с проверкой, кнопка «Вернуть стандартные ленты», SSRF-защита, автопроверка лент при старте; ленты читаются коллектором и монитором из БД — см. [20-news-sources-manager.md](./20-news-sources-manager.md)), **источник «Сайты компаний»** (страницы-списки новостей сайтов эмитентов: вкладка «Сайты» на `/news`, API/веб-роуты `kind=website`, коллектор `app/collectors/websites.py` с LLM-извлечением записей и обходом антибота, флаги `--sites` в `collect_news`/`daily_pipeline`, unit + e2e тесты — см. [22-company-sites-source.md](./22-company-sites-source.md)). Остаётся в этапе 1: английский язык, глубокий бэкфилл исторических новостей.

**Ограничение бэкфилла новостей:** RSS-ленты отдают только последние элементы (глубина — обычно от нескольких дней до недель), поэтому «полная» историческая загрузка через RSS невозможна. Глубокая история (месяцы/годы) доступна через архивы Telegram-каналов и поисковые API источников. Уже сейчас для ускорения старта скрипты `collect_news` и `daily_pipeline` поддерживают окно сбора: `--days N` (последние N дней) и `--from YYYY-MM-DD` (с указанной даты), а `collect_news --entity <имя>` — точечный сбор новостей по конкретным сущностям/бумагам.

## Этап 2. Углубление аналитики

**Цель:** повышение точности и доверия.

| Задача | Результат |
|---|---|
| Paper trading и сравнение с бенчмарком | Прозрачность реальной применимости |
| Научные источники в обоснованиях | Автоматический поиск и проверка ссылок |
| Обучение весов факторов на обратной связи | Версии весов, A/B-сравнение |
| Контраргументы и риски в автоматическом режиме | Полные evidence-цепочки |
| Расширение графа: 500+ сущностей, авто-кандидаты связей | Глубже покрытие отраслей |
| Модули биржевых индикаторов (см. [19-market-indicators.md](./19-market-indicators.md)) | Отдельные вызываемые индикаторы: пакет `app/market/indicators/`, REST API `/api/indicators/*`, веб-страница; MACD и уровни поддержки/сопротивления закрывают невыполненные пункты P0 FR-04-02 |
| **Volume Profile в аналитике бумаги** (POC, Value Area VAH/VAL, HVN, LVN) | Горизонтальный профиль объёма на карточке бумаги с выбором периода; уровни POC/VAH/VAL/HVN/LVN в «Уровнях» и «Сигналах»; техсигналы в движке стратегий (ТЗ: [19-market-indicators.md](./19-market-indicators.md) §8.13) |
| **Теханализ в LLM (ChatGPT)** | Кнопка «Теханализ в LLM» на карточке бумаги: дособор актуальных данных (цены; для фьючерсов — OI/клиентские группы) → формирование Markdown-запроса (данные + промт [promt_tech_analize.md](./promt_tech_analize.md)) → отправка в ChatGPT → карточки результатов (не более 4 на страницу + пагинация: дата, Купить/Продать/Выжидать, точка входа, TP, SL), страница полного ответа, скачивание запроса/ответа в MD, повтор неудачных запросов (ТЗ: [23-tech-analysis-llm.md](./23-tech-analysis-llm.md)) |
| **Актуальность рыночных данных в реальном времени** | Автономный демон обновляет live-котировки, дневные свечи и OI по составу watchlist+mvp+шаблон фьючерсов; live-котировки транслируются в браузер по SSE (`GET /v1/realtime/stream`); настройки (тумблер, интервалы, шаблон фьючерсов) — в админ-панели (ТЗ: [24-market-realtime.md](./24-market-realtime.md), **на согласовании**) |

**Статус этапа 2:** начат. **Paper trading реализован** (виртуальный портфель): виртуальный счёт, автоматические позиции по вердиктам BUY/SELL в ежедневном конвейере (вход = close дня вердикта, вариант А — без заглядывания в будущее), ручное закрытие и сброс, метрики (P&L, доходность, % прибыльных сделок, средний результат, максимальная просадка) и бенчмарк «купил всё и держишь»; API `/paper` и веб-страница `/paper`. **Контраргументы и риски реализованы** (FR-05-07): движок детерминированно формирует контраргументы (разнонаправленные сигналы, техсигналы RSI, ближайшие макрособытия) и риски (рыночные, событийные, отраслевые/корпоративные); при значимых контраргументах уверенность снижается; вывод в вебе, Telegram и API. **Обучение весов на обратной связи реализовано** (FR-06-05): версионируемые веса факторов (news/graph/counter_penalty), движок применяет текущую версию, калибровка по оценкам `scripts/calibrate_weights.py` создаёт новые версии (историчность, A/B-сравнение). **Биржевые индикаторы:** список из 12 индикаторов утверждён ([19-market-indicators.md](./19-market-indicators.md), v1.0). **Реализован OI полностью**: пакет `app/market/indicators/`, таблица `market_open_positions`, скрипт `update_oi.py` (`--ticker`/`--all` с полем дней, по умолчанию 30), API `/v1/indicators` (список, расчёт OI с сигналами «цена × OI», список всех фьючерсов `/futures`), админка (тикер-поле с подсказкой фьючерсов, поле «дней», чекбокс «--all» с блокировкой тикера, прогресс-бар), веб-страница `/indicators` (графики OI+цена и ΔOI, таблица сигналов), **интеграция в движок стратегий** (сигналы OI в контраргументах/рисках, ослабление net_score ×0.85, работает в бэктесте «на момент T»). Далее — остальные индикаторы (EMA/MACD — P0/P1). **EMA/MACD реализованы и интегрированы в движок стратегий** (техсигнал направления тренда — kind `trend`, ослабление net_score ×0.9 при противоречии знаку net_score; см. [09-strategy-engine.md](./09-strategy-engine.md) §4.1). **Volume Profile (POC/Value Area/HVN/LVN) реализован** ([19-market-indicators.md](./19-market-indicators.md) §8.13): модуль `volume_profile.py`, API `/v1/indicators/volume_profile`, профиль объёма на карточке бумаги с выбором периода (1M/2M/3M/6M/1Y), сигнал POC/Value Area в движке стратегий. **Поддержка/сопротивление реализовано** ([19-market-indicators.md](./19-market-indicators.md) §8.11, закрывает P0 FR-04-02): модуль `support_resistance.py` (pivot points + фракталы Уильямса + кластеризация по ATR, сила по касаниям), API `/v1/indicators/support_resistance`, веб-вкладка на `/indicators` (таблица уровней, pivot, сигналы пробоя/отскока), интеграция в движок (уровни в «Сигналах», сдерживающий фактор «цена у уровня»). **P1-индикаторы Боллинджер (BB), ATR, ADX/DI реализованы** ([19-market-indicators.md](./19-market-indicators.md) §8.3/8.8/8.10): модули `bollinger.py`/`atr.py`/`adx.py`, API `/v1/indicators/bollinger|atr|adx` (параметр `k` для BB), веб-вкладки на `/indicators`, интеграция в движок стратегий (BB — зоны перекупленности/перепроданности цены как сдерживающий фактор kind `bollinger`; ATR — информационный сигнал волатильности `atr_pct` kind `atr`; ADX — фильтр силы тренда kind `adx`, ослабление net_score ×0.9 при противоречии); этим закрыты P1-пункты FR-04-02. **E2E-тесты веб-интерфейса реализованы** ([21-web-e2e-tests.md](./21-web-e2e-tests.md)): Playwright, `tests/e2e/`, 42 сценария P0–P2 (публичные страницы, авторизация, приватные функции, news manager, админка, мобильное меню), отдельный прогон `pytest -m e2e`, реестр расхождений E2E-R1…R3. Осталось в этапе 2: **научные источники в обоснованиях — реализовано (FR-05-08)** (механика research-обоснований в движке + вывод в веб/API/Telegram; **дополнение ссылками — через `scripts/add_research.py` и админку «Дополнение графа» (`/admin/graph`)** (несколько ссылок за раз и/или загрузка PDF с LLM-анализом связи) с созданием новых рёбер/сущностей и идемпотентными прикреплениями; пилотный набор из 13 связей с реальными ссылками); **перенос графа между БД реализован** (`scripts/export_graph.py`/`scripts/import_graph.py` — JSONL-дамп сущностей и рёбер, идемпотентный импорт, `--dry-run`; восстановление после пересоздания/переезда БД, см. [13-operations.md](./13-operations.md) «Перенос графа между БД»), **карта зависимостей реализована** (серверная SVG-карта цепочек влияния вокруг бумаги из knowledge graph: причина → … → сектор → бумага; на стрелках — механизм передачи (`rationale` ребра) и знак ±; под узлами — подсказки «что отслеживать» (`entity_watch_metrics`, пилот 24 метрики через `scripts/seed_watch_metrics.py`); карта на карточке `/securities/{ticker}` и отдельная страница `/map`). **Расширение графа** (кураторская задача: добавление компаний/связей — через названный инструмент; критерий полноты — охват ликвидных акций МосБиржи из `seed_db` и наличие отраслевых связей для каждой компании; новые связи вносятся в `app/graph/seed_data.py` или добавляются через инструмент). **Теханализ в LLM (ChatGPT) реализован** (ТЗ: [23-tech-analysis-llm.md](./23-tech-analysis-llm.md)): кнопка «Теханализ в LLM» на карточке акций/фьючерсов `/securities/{ticker}`, панель этапов (актуализация данных → формирование запроса → отправка в LLM), конфиг `CHATGPT_*` (`CHATGPT_API_KEY` из общих настроек), модель `tech_analyses` + миграция `021`, сервис `app/tech_analysis` (этапы, повтор с тем же запросом, SystemNotice при сбое, stale-записи), страница ответа `/tech_analysis/{id}` (карточки сценариев A/B/C из «Моей стратегии», рендер Markdown, «Скачать запрос/ответ (MD)», «Повторить»), карточки результатов ≤4/стр. с пагинацией; недостающие индикаторы реализованы: **RSI** ([19](./19-market-indicators.md) §8.15), **Базис «фьючерс-спот»** (§8.16); e2e-тесты `tests/e2e/tech-analysis.spec.ts`. Дальний TODO: по клику на карточку сценария — авто-выставление ордеров у реального брокера (после интеграции с брокером).


> **Низкоприоритетная задача (возможно, не нужна — подумать):** фаза Ежедневного конвейера «Расчёт индикаторов» (актуализация индикаторов EMA/MACD/OI/Volume Profile/Support-Resistance/Bollinger/ATR/ADX по каждому инструменту). Сейчас значения индикаторов считаются на лету при обращении к API/вебу, отдельный проход не требуется — прежде чем реализовывать, перепроверить необходимость.

> **Низкоприоритетная опция — реализована (шаг выполнен):** интерактивная JS-визуализация карты зависимостей. **Cytoscape.js внедрён на странице `/map`** (зум колесом, панорамирование drag, направленные рёбра со стрелкой и подписью-механизмом, раскраска узлов по типу/цели; библиотека — локально `/static/cytoscape.min.js`). На карточке бумаги осталась серверная SVG-версия (быстрая, для скриншота). Подсветка/тултипы/более глубокая интерактивность (по желанию) — можно расширять позже.

## Этап 3. Масштабирование и интеграции

**Цель:** рост объёмов и внешние интеграции.

| Задача | Результат |
|---|---|
| Мультиязычность полная (RU/EN интерфейс) | Доступность |
| Публичное API с песочницей | Внешние разработчики |
| Интеграция с брокерскими платформами (read-only) | Импорт реального портфеля |
| Подписки/тарифы (для коммерциализации) | Модель монетизации |

## Этап 4. Исследовательский горизонт

- Автоматическое открытие новых связей графа по корпусу статей с кураторским подтверждением.
- Модель «реакции рынка»: прогноз краткосрочного движения цены на событие.
- Транскрибация видео/подкастов.
- Персональная адаптация вердиктов под стиль и рисковый профиль пользователя.

## Дальняя перспектива (за рамками этапов)

Крупные пользовательские задачи, требующие отдельного проектирования. Реализация не начата; при старте задачи — зафиксировать дизайн (обоснование, FR/UC, модель данных, API, UI) в `docs/` и показать на подтверждение (см. [16-working-process.md](./16-working-process.md), правило 6).

### Графический редактор графа влияний + актуальность графа

**Обоснование (as-is):** текущий способ ведения графа — карточки связей в админке «Дополнение графа» (`/admin/graph`). Карточки не визуальны, разрозненны и фрагментарны: одна карточка = одна связь («Нефть → X5», «Ставка ЦБ → X5», «Долги → X5»), по ним сложно ориентироваться и видеть целостную картину влияний на компанию/сектор. Дополнительно граф не актуализируется оперативно: рёбра не меняются под влиянием новостей, новых отчётностей и геополитики.

**Цель (to-be):** графический редактор графа влияний, в котором пользователь визуально строит связи бизнеса с миром. **Один граф = одна история про компанию или сектор** (полностью описывает его связность с миром/сущностями); если граф для компании/сектора уже существует, второй создать нельзя. Граф должен быть **актуальным**: новости, новые отчётности, геополитика оперативно меняют рёбра (знак, силу, наличие).

**Сценарии (FR/UC):**

- **UC-1. Создание сущности вручную.** Пользователь создаёт сущность (например «Нефть»). Сущности — переиспользуемые «кирпичики» ценообразования, часто входят в построение графов к секторам и компаниям.
- **UC-2. Связывание сущности с компанией или сектором.** Например, «Нефть» → X5 (компания) или «Нефть» → «Ритейл» (сектор).
- **UC-3. Графическое добавление связи.** Связь добавляется стрелкой (графически), выставляется влияние на компанию/сектор и направление: положительное (синий) / отрицательное (красное).
- **UC-4. Опциональная ссылка.** Ссылка на статью/источник, подтверждающая связь, не обязательна.
- **UC-5. Выбор бизнеса и сущностей.** Пользователь добавляет конкретный бизнес (выбор из акций МосБиржи) и ведёт от него связи к сущностям — готовым или созданным вручную (ставка ЦБ, Нефть, Пшеница, Долг, капекс, Спрос, драгметаллы, другие экономические показатели).
- **UC-6. Влияние и обоснования вручную или через RAG LLM.** Значение влияния и обоснование можно задать вручную, либо RAG LLM выделяет их из отчётности компании, которую пользователь подгружает или которая уже есть в библиотеке (документы, БД, API, Wiki, корпоративные знания).
- **UC-7. Описание на рёбрах.** На ребре — краткое описание «что произошло» + ссылка на место в документе отчётности компании или иной источник (например, красное ребро «Нефть → X5» с пометкой «Резкое подорожание цен на бензин» и ссылкой).
- **UC-8. Актуальность графа.** Новости, новые отчётности, геополитика оперативно меняют рёбра графа: смена знака (например, «Цена на нефть высокая» — отрицательное влияние → «Цена упала» — положительное), изменение силы, добавление/удаление рёбер.
- **UC-9. Алерты при сильных изменениях графа.** Пользователь оперативно информируется алертами (веб/Telegram/API) при значимом изменении: смена знака влиятельного ребра, резкое изменение силы, добавление/удаление важной связи.

**Механизм сверки актуальности (открытый вопрос, механизм разработать):** желаемый режим — «в реальном времени», но конкретный механизм не определён; варианты для проектирования: периодическая сверка по расписанию, слежка за RSS-лентами по сущностям графа, ручная сверка, RAG LLM по отчётности (новые отчётности меняют рёбра). Выбор механизма и периодичности — на этапе проектирования.

**Правило приоритета:** при разногласиях по связи приоритет у связи **компании** над связью **сектора**; если у компании связи нет — используются данные сектора (общие связи сектора, к которому относится компания).

**Статус:** запланировано (дальняя перспектива).

## Критерии перехода между этапами

- Каждый этап завершается замером метрик NFR-04 и демонстрацией ключевых сценариев.
- Гейт на выходе из этапа: отсутствие критических дефектов, метрики качества не хуже целевых, документация актуальна.
