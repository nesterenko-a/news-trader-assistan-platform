{
  "name": "newstrader-e2e",
  "private": true,
  "description": "E2E-тесты веб-интерфейса NewsTrader Assistant (Playwright Test Runner)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
